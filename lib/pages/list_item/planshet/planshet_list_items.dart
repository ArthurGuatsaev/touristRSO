import 'package:flutter/material.dart';

class PlanshetListItems extends StatelessWidget {
  const PlanshetListItems({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.blue,);
  }
}