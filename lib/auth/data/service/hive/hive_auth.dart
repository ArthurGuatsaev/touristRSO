class HiveIsAuthService {
  Future<Map<String, String>> isAuth() async {
    return {};
  }
  Future<void> saveAuthData({required String name, required String email}) async {
    //final openBox = Hive.openBox('user');
    //openBox.save(name, email);
  }
}